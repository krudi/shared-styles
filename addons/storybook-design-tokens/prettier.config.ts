import type { Options } from 'prettier';

const config: Options = {
  singleQuote: true,
  trailingComma: 'all',
  arrowParens: 'always',
  printWidth: 120,
  tabWidth: 2,
  useTabs: false,
};

export default config;
