name: Publish styles

on:
  release:
    types: [published]

  workflow_dispatch:

concurrency:
  group: publish-styles-${{ github.ref }}
  cancel-in-progress: true

jobs:
  publish:
    if: ${{ github.event_name == 'workflow_dispatch' || startsWith(github.event.release.tag_name, '@krudi/styles@') }}
    permissions:
      contents: read
      id-token: write
    uses: krudi/reusable-actions/.github/workflows/publish-npm.yaml@main
    with:
      workspace: "@krudi/styles"
      node_version_file: .nvmrc
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
