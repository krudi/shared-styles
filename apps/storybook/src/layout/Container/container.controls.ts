import type { Meta } from '@storybook/html-vite';

import type { ContainerArgs } from './container.types';

export const defaultArgs: ContainerArgs = {};

export const argTypes: Meta<ContainerArgs>['argTypes'] = {};
