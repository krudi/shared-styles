import type { Meta } from '@storybook/html-vite';

import type { CheckboxArgs } from './checkbox.types';

export const defaultArgs: CheckboxArgs = {};

export const argTypes: Meta<CheckboxArgs>['argTypes'] = {};
