# @krudi/storybook

Storybook workspace for `@krudi/styles` using HTML stories.

## Scripts

```sh
npm run storybook        # start Storybook on 6006
npm run storybook:build  # build static Storybook
```

Stories live in `stories/` and the global preview imports `../../styles/src/styles.css`.
